<UserControl x:Class="topinsApplication.tpOptions"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:topinsApplication"
             mc:Ignorable="d" 
             Height="450" Width="800">
    <UserControl.Resources>
        <DataTemplate DataType="{x:Type local:tpGeneralSettingMvvm}">
            <local:tpGeneralSetting />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:tpIPCameraSettingMvvm}">
            <local:tpIPCameraSetting />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:tpSerialSettingMvvm}">
            <local:tpSerialSetting />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:tpPresetSettingMvvm}">
            <local:tpPresetSetting />
        </DataTemplate>
        <DataTemplate DataType="{x:Type local:tpNetworkSettingMvvm}">
            <local:tpNetworkSetting />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1"/>
            <RowDefinition/>
            <RowDefinition Height="5"/>
            <RowDefinition Height="28"/>
            <RowDefinition Height="1"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1"/>
            <ColumnDefinition/>
            <ColumnDefinition Width="1"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Row="1" Grid.Column="1">
            <TabControl ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Header}" />
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <ContentControl Content="{Binding Content}" />
                    </DataTemplate>
                </TabControl.ContentTemplate>
                <TabControl.ItemContainerStyle>
                    <Style TargetType="TabItem" BasedOn="{StaticResource tbTabItem}">
                        <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
                    </Style>
                </TabControl.ItemContainerStyle>
            </TabControl>
        </DockPanel>

        <StackPanel Grid.Row="2" Grid.Column="1">
            <Separator Height="3" Background="#FF3D3D3D"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Apply" IsEnabled="{Binding IsApplyEnabled}" Command="{Binding ApplyWindow}" Width="80" Margin="0,4,6,2">
                
            </Button>
            <Button Content="OK" Width="80" IsEnabled="{Binding IsOKEnabled}" Command="{Binding ApplyWindow}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Margin="6,4,6,2">
                
            </Button>
            <Button Content="Cancel" Command="{Binding CloseWindow}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Width="80" Margin="6,4,0,2">
                
            </Button>
        </StackPanel>
    </Grid>
</UserControl>

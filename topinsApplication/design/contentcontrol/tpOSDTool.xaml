<UserControl x:Class="topinsApplication.tpOSDTool"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:topinsApplication"
             xmlns:d="http://schemas.microsoft.com/expression.blend/2008"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">

    <UserControl.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Tag" Value="{DynamicResource SystemButtonsMousePressedBackground}" />
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="True">
                    <Setter Property="Tag" Value="#FF4692CC"/>
                </Trigger>

                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Tag" Value="#FF1A7FCC"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Tag" Value="#FF0E5A9C"/>
                </Trigger>
                
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="1"/>
                <RowDefinition Height="24"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="28"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="1"/>
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="1" Grid.Row="2">
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,2,0,0">
                                <Grid.RowDefinitions>
                                    <RowDefinition/>
                                    <RowDefinition Height="5"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="32"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                    <TextBlock Text="{Binding Name}" Typography.NumeralAlignment="Tabular" FontWeight="Bold" TextAlignment="Right" Margin="3,4,6,0" Width="120"/>
                                    <ContentControl Content="{Binding}" Margin="6,0">
                                        <ContentControl.Style>
                                            <Style TargetType="ContentControl">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding InputType}" Value="{x:Static local:eINPUTTYPE.COMBOBOX}">
                                                        <Setter Property="ContentTemplate">
                                                            <Setter.Value>
                                                                <DataTemplate>
                                                                    <StackPanel IsEnabled="{Binding DataContext.IsFieldEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                                                        <ComboBox IsEnabled="{Binding IsEnabled}" ItemsSource="{Binding Items}" SelectedItem="{Binding SelectedItem, Mode=TwoWay}" DisplayMemberPath="Description" Width="120"/>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding InputType}" Value="{x:Static local:eINPUTTYPE.TEXTBOX}">
                                                        <Setter Property="ContentTemplate">
                                                            <Setter.Value>
                                                                <DataTemplate>
                                                                    <StackPanel IsEnabled="{Binding DataContext.IsFieldEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                                                        <TextBox IsEnabled="{Binding IsEnabled}" Text="{Binding SelectedItem.Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" Width="120">
                                                                            <i:Interaction.Behaviors>
                                                                                <local:tpNumericInputBehavior/>
                                                                                <local:tpTextBoxTextChangedBehavior Command="{Binding TextChanged}"/>
                                                                            </i:Interaction.Behaviors>

                                                                            <i:Interaction.Triggers>
                                                                                <i:EventTrigger EventName="LostFocus">
                                                                                    <i:InvokeCommandAction Command="{Binding LostFocus}"/>
                                                                                </i:EventTrigger>
                                                                            </i:Interaction.Triggers>
                                                                        </TextBox>
                                                                    </StackPanel>
                                                                </DataTemplate>
                                                            </Setter.Value>
                                                        </Setter>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ContentControl.Style>
                                    </ContentControl>
                                    <StackPanel IsEnabled="{Binding DataContext.IsFieldEnabled, RelativeSource={RelativeSource AncestorType=UserControl}}">
                                        <Button IsEnabled="{Binding IsCommandEnabled}" Style="{StaticResource ButtonStyle}" Command="{Binding Command}" CommandParameter="{Binding CommandParameter}" Width="16" Height="16" Margin="6,0,6,0">
                                            <Path Data="{DynamicResource Send}" Fill="{Binding Tag, RelativeSource={RelativeSource AncestorType=Button}}" Stretch="Uniform"/>
                                        </Button>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding CurrentValue}" Typography.NumeralAlignment="Tabular" TextAlignment="Right" Margin="0,6,8,0" FontWeight="Bold" FontSize="10" Foreground="#88FFFFFF"/>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="2" Visibility="{Binding RebootIcon, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Right" ToolTip="Need to restart.">
                                    <Viewbox Width="14" Height="14" Margin="0,5,0,0">
                                        <Path Data="{DynamicResource Reboot}" Fill="#FFAE0000" Stretch="Uniform"/>
                                    </Viewbox>
                                </StackPanel>
                                <StackPanel Grid.Row="0" Grid.Column="3" Visibility="{Binding BlockIcon, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Center" ToolTip="Don't change!!">
                                    <Viewbox Width="14" Height="14" Margin="0,5,0,0">
                                        <Path Data="{DynamicResource Block}" Fill="#FFAE0000" Stretch="Uniform"/>
                                    </Viewbox>
                                </StackPanel>

                                <StackPanel Grid.Row="0" Grid.Column="4" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <TextBlock Text="{Binding Range}" Typography.NumeralAlignment="Tabular" TextAlignment="Right" Margin="0,6,8,0" FontWeight="Bold" FontSize="10" Foreground="#858585"/>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="5">
                                    <Separator Height="3" Background="#FF3D3D3D"/>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Default" IsEnabled="{Binding IsDefaultEnabled}" Command="{Binding DefaultValue}" Width="80" Margin="6,4,0,2">
                    
                </Button>
                <Button Content="OK" Command="{Binding CloseWindow}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Width="80" Margin="6,4,0,2"/>
            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
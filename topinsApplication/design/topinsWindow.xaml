<Window x:Class="topinsApplication.topinsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:topinsApplication"
        mc:Ignorable="d"
        Title="topinsWindow" Height="420" Width="320" Background="{DynamicResource HeaderBackground}" Foreground="{DynamicResource WindowForeground}" SnapsToDevicePixels="True">

    <WindowChrome.WindowChrome>
        <!--<WindowChrome x:Name="chrome" ResizeBorderThickness="8" CornerRadius="0" GlassFrameThickness="0" NonClientFrameEdges="None" />-->
        <WindowChrome x:Name="chrome" ResizeBorderThickness="8" />
    </WindowChrome.WindowChrome>
    
    <Grid>
        <Grid.Style>
            <Style TargetType="{x:Type Grid}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding WindowState, RelativeSource={RelativeSource AncestorType={x:Type Window}}}" Value="Maximized">
                        <Setter Property="Margin" Value="6,6,6,8" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <Grid x:Name="header">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="4"/>
                <RowDefinition />
                <RowDefinition Height="4"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="4"/>
                <ColumnDefinition/>
                <ColumnDefinition Width="4"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" Grid.Row="0" Margin="8,4" HorizontalAlignment="Left" Orientation="Horizontal">
                <Border Padding="4,0" Margin="14,0">
                    <TextBlock Text="{Binding CaptionName}" VerticalAlignment="Center" Padding="6,0,7,3">
                        <TextBlock.Style>
                            <Style TargetType="{x:Type TextBlock}">
                                <Setter Property="Foreground" Value="{DynamicResource ProjectNameForeground}"/>
                                <Setter Property="Background" Value="{DynamicResource ProjectNameBackground}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsActive, RelativeSource={RelativeSource AncestorType={x:Type Window}, Mode=FindAncestor}}" Value="False">
                                        <Setter Property="Foreground" Value="{DynamicResource ProjectNameInactiveForeground}" />
                                        <Setter Property="Background" Value="{DynamicResource ProjectNameInactiveBackground}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>
            </StackPanel>
            <StackPanel  Grid.Column="1" Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" WindowChrome.IsHitTestVisibleInChrome="True">
                <Button Style="{StaticResource MinimizeButton}" Click="OnMinimizeClicked" IsEnabled="False" />
                <Button Style="{StaticResource MaximizeButton}" Click="OnMaximizeClicked" IsEnabled="False" />
                <Button Style="{StaticResource RestoredButton}" Click="OnRestoredClicked" IsEnabled="False" />
                <Button Style="{StaticResource ShutDownButton}" Click="OnShutDownClicked" />
            </StackPanel>
            <StackPanel Grid.Column="1" Grid.Row="2" Orientation="Vertical">
                <ContentControl Content="{Binding CurrentViewModel}" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
